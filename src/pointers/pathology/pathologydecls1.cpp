#include <iostream>
using namespace std;
//start

int main() {
    int a, b, c;  /* Zgodnie z oczekiwaniami ta linia tworzy trzy zmienne typu int. */
    int* d, e, f; /* Ta linia tworzy jeden wskaźnik do zmiennej typu int i dwie zmienne typu int. */
    int *g, *h;   /* Ta linia tworzy dwa wskaźniki do zmiennych typu int. */
    int* i, * j;  /* Ta linia również tworzy dwa wskaźniki do zmiennych typu int.  */

    return 0;
}
